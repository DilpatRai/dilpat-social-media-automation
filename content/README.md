# Post Content Packages

Each generated post can have a Markdown package under `content/YYYY/MM/` using its request ID as the filename.

A package should contain the topic, request ID, platform captions, alt text, status, and references to the generated media.
